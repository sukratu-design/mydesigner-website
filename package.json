{
  "name": "mydesigner-website",
  "private": true,
  "version": "1.0.0",
  "description": "MyDesigner marketing site and customer portal",
  "scripts": {
    "build": "npm run build:blog && npm run build:portfolio && npm run sync:indexes",
    "build:blog": "node scripts/build-blog.js",
    "build:portfolio": "node scripts/build-portfolio.js",
    "sync:indexes": "node scripts/sync-directory-indexes.js",
    "preview": "node scripts/preview-server.js",
    "test": "node scripts/check-vs-positioning.js && node scripts/check-contact-page.js && node scripts/check-service-detail-pages.js && node scripts/check-work-case-study-pages.js && node scripts/check-portfolio-category-pages.js && node scripts/check-blog-positioning.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.1",
    "gray-matter": "^4.0.3",
    "marked": "^16.3.0",
    "stripe": "^18.0.0"
  }
}
